import 'package:flutter/material.dart';

class CustomUnknownScreen extends StatelessWidget {
  const CustomUnknownScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
